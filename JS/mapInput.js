/*class MapSelect {
    constructor(maps) {
        this.maps = maps
    }

    clearAll() {
        selectMap = undefined;
    }

    getSelectedMaps() {
        for (let i = 1; i < 24; i++) { 
            selectMap = document.getElementById("S" + i);
            if (selectMap = true) {
                SZMaps.push(maps[i-1])
            }
        }

        for (let i = 1; i < 24; i++) { 
            selectMap = document.getElementById("T" + i);
            if (selectMap = true) {
                TCMaps.push(maps[i-1])
            }
        }

        for (let i = 1; i < 24; i++) { 
            selectMap = document.getElementById("R" + i);
            if (selectMap = true) {
                RMMaps.push(maps[i-1])
            }
        }

        for (let i = 1; i < 24; i++) { 
            selectMap = document.getElementById("C" + i);
            if (selectMap = true) {
                CBMaps.push(maps[i-1])
            }
        }

    }

}






const mapselect = new MapSelect(maps)



*/